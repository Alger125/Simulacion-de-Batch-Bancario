<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:batch="http://www.springframework.org/schema/batch"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/batch
           http://www.springframework.org/schema/batch/spring-batch.xsd">

    <import resource="classpath:META-INF/spring/batch/beans/lox-beans.xml" />

    <batch:job id="LOXJ162-01-MX" job-repository="jobRepository">

        <batch:step id="step1">
            <batch:tasklet transaction-manager="transactionManager">
                <batch:chunk reader="itemReaderJob"
                             processor="itemProcessorJob"
                             writer="itemWriterJob"
                             commit-interval="100"/>
            </batch:tasklet>

            <batch:next on="COMPLETED" to="stepCompleted"/>
            <batch:next on="FAILED" to="stepFailed"/>
            <batch:next on="*" to="stepFailed"/>
        </batch:step>

        <batch:step id="stepCompleted">
            <batch:tasklet ref="beanCompletedJob"/>
        </batch:step>

        <batch:step id="stepFailed">
            <batch:tasklet ref="beanFailedJob"/>
        </batch:step>

    </batch:job>

</beans>